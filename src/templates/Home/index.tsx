import React from "react";
import { ProductList } from "../../containers/ProductList";

const Home = (): JSX.Element => {
  return (
    <div>
      <ProductList />
    </div>
  );
};

export { Home };
